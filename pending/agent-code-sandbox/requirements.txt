# Dev / test / lint dependencies for agent-code-sandbox.
# Install the package itself in editable mode with the api extra:
#   pip install -e ".[api]" -r requirements.txt
pytest>=7.4,<9.0
pytest-cov>=4.1,<6.0
ruff>=0.4,<1.0
mypy>=1.8,<2.0
httpx>=0.27,<1.0
fastapi>=0.100,<1.0
uvicorn>=0.23,<1.0
