# Runtime dependencies: none -- agentflow's core uses only the Python
# standard library (sqlite3, json, argparse, threading).
#
# Dev/test dependencies:
pytest>=8.0,<9
pytest-cov>=5.0,<6
ruff>=0.6,<1.0
mypy>=1.10,<2
