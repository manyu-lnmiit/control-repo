# Path to the SQLite database file used to store spans/traces.
AGENT_TRACE_LENS_DB=agent_trace_lens.db

# Host/port the web server binds to when run via `agent-trace-lens serve`.
AGENT_TRACE_LENS_HOST=0.0.0.0
AGENT_TRACE_LENS_PORT=8000
