# Path to the SQLite database file used by the CLI and HTTP server.
# Use ":memory:" for an ephemeral store (e.g. in tests / CI).
AGENT_MEMORY_DB=agent_memory.db

# Host/port used by `agent-memory-store serve` (also settable via CLI flags).
AGENT_MEMORY_HOST=0.0.0.0
AGENT_MEMORY_PORT=8000
